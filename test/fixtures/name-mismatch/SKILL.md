---
name: wrong-name
description: This fixture has a name that does not match its parent folder, so it should fail every validation profile regardless of description length limits.
license: MIT
---

# name-mismatch

The frontmatter name is `wrong-name` but the folder is `name-mismatch`.
